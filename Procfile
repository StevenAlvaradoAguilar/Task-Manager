web: rake db:migrate && rails db:features:load && bin/rails server -b 0.0.0.0 -p ${port:-3000} 
# web: rake db:migrate && rake fetch_sismic_data:execute && bin/rails server -b 0.0.0.0 -p ${PORT:-3000}
