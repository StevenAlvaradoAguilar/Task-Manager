#web rake db:migrate && rails fetch_sismic_data:execute && bin/rails server -b 0.0.0.0 -p ${port:-3000} 
web: bundle exec rake db:migrate && bundle exec rails fetch_sismic_data:execute && bundle exec rails server -b 0.0.0.0 -p ${PORT:-3000}
